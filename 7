7. Определить, имеется ли среди заданных целых чисел A, B, C хотя бы одно чётное.
#include <stdio.h>
int main(){
    int a,b,c;
    scanf("%d %d %d", &a, &b, &c);
    if(a%2==0 || b%2==0 || c%2==0)
        printf("Да, имеется");
    else
        printf("Нет, не имеется");
}
